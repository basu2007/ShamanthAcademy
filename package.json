{
  "name": "shamanth-academy",
  "version": "1.0.0",
  "description": "Professional Technical Learning Platform",
  "main": "index.js",
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "esbuild index.tsx --bundle --outfile=index.js --format=esm --jsx=automatic --minify --external:react --external:react-dom"
  }
}